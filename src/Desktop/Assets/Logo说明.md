# 程序图标

“声纹 + 文本光标”的组合表示按住说话后转为文字输入。青绿色与程序已有配色一致。

- `Logo.svg`：512 × 512 viewBox 的矢量原稿，可无损缩放。
- `Logo.png`：1,024 × 1,024 透明 PNG。
- `AppIcon.ico`：Windows 图标，含 9 个尺寸（16 至 256 像素）。

主色 `#226E67`，声纹白色，光标 `#BCE9D4`。EXE、窗口和托盘共用此标识。图形为本项目新绘制的几何矢量，不依赖字体或外部图片。
